package io.netbird.client.tool.networks;

/**
 * NetworkToggleListener defines a callback to be fired when
 * the device's currently available network type changes.
 */
public interface NetworkToggleListener {
    void onNetworkTypeChanged();
}
