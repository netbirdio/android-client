<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>
    <dimen name="nav_header_vertical_spacing">8dp</dimen>
    <dimen name="nav_header_height">176dp</dimen>
    <dimen name="fab_margin">16dp</dimen>
    <dimen name="dialog_horizontal_margin">16dp</dimen>
    <dimen name="dialog_max_width">560dp</dimen>
    <dimen name="fragment_max_width">500dp</dimen>
    <!-- Bottom-nav reserved space; scrollable fragments add this as bottom padding so content doesn't disappear behind the nav bar. -->
    <dimen name="bottom_nav_inset">80dp</dimen>
    <!-- Same, for fragments with a FAB: the nav bar (80dp) plus the button sitting
         above it (56dp) and a gap so the last card clears both. -->
    <dimen name="bottom_nav_fab_inset">152dp</dimen>
</resources>
