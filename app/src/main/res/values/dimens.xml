<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>
    <dimen name="nav_header_vertical_spacing">8dp</dimen>
    <dimen name="nav_header_height">176dp</dimen>
    <dimen name="fab_margin">16dp</dimen>
    <dimen name="dialog_horizontal_margin">16dp</dimen>
    <dimen name="dialog_max_width">560dp</dimen>
    <dimen name="fragment_max_width">500dp</dimen>
    <!-- List bottom padding on screens with a FAB: the button (56dp), its margin
         (16dp) and a gap so the last card clears it. -->
    <dimen name="fab_content_inset">88dp</dimen>
    <!-- Gap under the last peer card on the share screen, before the navigation
         bar inset is added on top of it. -->
    <dimen name="share_list_bottom_padding">16dp</dimen>
</resources>
