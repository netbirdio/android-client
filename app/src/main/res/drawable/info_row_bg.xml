<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded outlined container for the connection info rows (IP address, etc.) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="12dp" />
    <solid android:color="@color/info_row_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/info_row_stroke" />
</shape>
