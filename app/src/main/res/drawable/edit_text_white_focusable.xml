<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- When focused, show white background with an accent border. A 3dp near
         black stroke reads as a heavy box, so the highlight is thinner and
         tinted instead, matching the night variant. -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <stroke
                android:width="1dp"
                android:color="@color/nb_orange" />
            <corners android:radius="2dp" />
        </shape>
    </item>

    <!-- Default state: white background with 1dp gray border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <stroke
                android:width="1dp"
                android:color="#ececec" />
            <corners android:radius="2dp" />
        </shape>
    </item>
</selector>
