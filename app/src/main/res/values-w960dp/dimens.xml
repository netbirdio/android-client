<resources>
    <!-- On w960dp+ the bottom nav becomes a side rail (see layout-w960dp/activity_main.xml),
         so fragments don't need to reserve vertical space for it. -->
    <dimen name="bottom_nav_inset">0dp</dimen>
    <!-- The FAB still sits in the bottom corner here, so its own clearance remains. -->
    <dimen name="bottom_nav_fab_inset">72dp</dimen>
</resources>
